import 'package:flutter/material.dart';
import 'package:weatherlogic/constants/app_constants.dart';

class AppTextStyle {
  static const appBar = TextStyle(color: AppColors.black, fontSize: 28);

  static const centertext = TextStyle(color: Colors.white, fontSize: 70);

  // static  body(double size) => TextStyle(color: Colors.white, fontSize: size);
}
